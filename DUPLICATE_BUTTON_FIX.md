# Bloom — duplicate Partner buttons fixed

The Partner Home had the Baby Tracker / Mum's Updates action row rendered
twice. This package removes the duplicate while preserving the shared Baby
Tracker access.

Partner Home now has one:
- Open Baby Tracker
- View Mum's Updates

No Firebase changes are required for this UI-only fix.
